import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import katex from 'katex';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),rel='courses/linear-algebra/2026-tb1/week-01',out=root+'/'+rel,source=root+'/content/linear-algebra/2026-tb1/week-01';
const ref=JSON.parse(fs.readFileSync(source+'/reference.json')),script=JSON.parse(fs.readFileSync(source+'/script.json'));
const esc=s=>s.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;');const rich=s=>s.split(/(\$\$[\s\S]*?\$\$|\$[^$]+\$)/g).map(p=>p.startsWith('$')?katex.renderToString(p.slice(p.startsWith('$$')?2:1,p.startsWith('$$')?-2:-1),{displayMode:p.startsWith('$$'),throwOnError:true}):esc(p)).join('');const svg=f=>fs.readFileSync(root+'/'+f,'utf8');const grid=svg('diagrams/blank-coordinate-grid.svg');
const css=`@page{size:A4;margin:13mm 15mm}*{box-sizing:border-box}body{margin:0;color:#172b3a;font-family:Arial,'PingFang SC',sans-serif;font-size:11pt;line-height:1.4}.page{break-after:page;position:relative;min-height:265mm}.page:last-child{break-after:auto}h1{font-size:20pt;margin:0 0 5mm}h2{font-size:14pt;margin:4mm 0 2mm}h3{font-size:11pt;margin:3mm 0}.meta{font-size:9pt;color:#394c5b;border-bottom:1px solid #9aaab7;padding-bottom:3mm}.intro{font-size:10pt}.question{font-size:11pt;line-height:1.6}.katex{font-size:1.03em}.grid{width:85mm;height:46mm;display:inline-block}.grid svg{height:100%!important;width:100%!important}.line-space{background:repeating-linear-gradient(to bottom,transparent 0,transparent 8.7mm,#d9dee2 8.8mm,transparent 9mm);height:40mm}.box{border:1px solid #bcc7cf;border-radius:2mm;padding:3mm;margin:3mm 0}.label{font-size:9pt;color:#4c5c68}.row{display:flex;gap:6mm}.row>*{flex:1;min-width:0}.small{font-size:9pt}.foot{position:absolute;bottom:0;left:0;right:0;font-size:8pt;border-top:1px solid #bcc7cf;padding-top:2mm;display:flex;justify-content:space-between}.teacher .diagram{margin:4mm auto;width:100%;max-width:170mm}.teacher .diagram svg{width:100%;max-height:86mm}.teacher ol{padding-left:6mm;margin:3mm 0}.teacher li{margin:3mm 0;break-inside:avoid}.zh{font-size:10pt;color:#425970;margin-top:1mm}.formula{margin:3mm 0;padding:3mm;background:#f2f5f7;font-size:12pt}.notice{border-left:3px solid #596f82;padding-left:3mm;font-size:10pt}.writing{height:30mm;border:1px solid #ccd4da;margin:3mm 0}.question .katex-display{margin:2mm 0}`;
function shell(title,pages,teacher=false){return `<!doctype html><html lang="${teacher?'zh-CN':'en'}"><head><meta charset="utf-8"><meta name="robots" content="noindex,nofollow"><title>${title}</title><link rel="stylesheet" href="../../../../vendor/katex/katex.min.css"><style>${css}</style></head><body class="${teacher?'teacher':'student'}">${pages.join('')}</body></html>`;}
const q=id=>rich(ref.find(e=>e.id===id).questionEn);const foot=(n,type)=>`<footer class="foot"><span>Linear Algebra · Week 1 · ${type} · Activity labels A1–A6 are local labels</span><span>${n}</span></footer>`;const head=(title,note)=>`<h1>${title}</h1><div class="meta">Linear Algebra · Week 1 · 25 September 2026<br>${note}</div>`;
const student=[
`<section class="page">${head('Try · Compare · Explain','Student worksheet — no solutions | Name (optional): __________________')}<p class="intro">Try each part on your own first, then compare your approach with a partner. Show your working. If stuck, write what you tried and where the difficulty starts. You do not need to finish every activity.</p><h2>A1 · A complex number as a picture</h2><div class="question">${q('A1')}</div><p class="small">Start by plotting it and finding its distance from the origin. If you have met polar form, give that too. Label your scale.</p><div class="row"><div class="grid">${grid}</div><div><div class="label">Position, distance, and another representation:</div><div class="line-space"></div></div></div><h2>A2 · Vector calculations</h2><div class="question">${q('A2')}</div><div class="row"><div><h3>(a) Find x.</h3><div class="line-space" style="height:40mm"></div></div><div><h3>(b) Find y. Check by substitution.</h3><div class="line-space" style="height:40mm"></div></div></div><p class="small">Explain one step to your partner. What did you check?</p>${foot(1,'Student copy')}</section>`,
`<section class="page">${head('Directions and coordinates','Student worksheet — no solutions')}<h2>A3 · Polar to Cartesian</h2><div class="question">${q('A3')}</div><p class="small">Sketch the direction and length, then calculate both components. Check the length of your answer.</p><div class="row"><div class="grid">${grid}</div><div><div class="line-space" style="height:48mm"></div></div></div><h2>A4 · Cartesian to polar</h2><div class="question">${q('A4')}</div><div class="row"><div><h3>Vector (3, 0)</h3><div class="grid">${grid}</div><p>λ = __________ &nbsp; θ = __________</p></div><div><h3>Vector (0, −3)</h3><div class="grid">${grid}</div><p>λ = __________ &nbsp; θ = __________</p></div></div><div class="box"><span class="label">Explain how you chose each angle. Does it satisfy the required range?</span><div class="line-space" style="height:24mm"></div></div>${foot(2,'Student copy')}</section>`,
`<section class="page">${head('Complex arithmetic and a final check','Student worksheet — no solutions')}<h2>A5 · Write a complex number in standard form</h2><div class="question">${q('A5')}</div><div class="box"><div class="label">Simplify the numerator.</div><div class="line-space" style="height:22mm"></div></div><div class="box"><div class="label">Rewrite the fraction in the requested form. Explain why each step preserves its value.</div><div class="line-space" style="height:32mm"></div></div><div class="box"><div class="label">Check your result by multiplying by the original denominator.</div><div class="line-space" style="height:18mm"></div></div><h2>A6 · Optional — only if covered in lectures</h2><div class="question">${q('A6')}</div><div class="line-space" style="height:28mm"></div><div class="box"><strong>Exit check — do this independently.</strong><p class="small">Write (0, −2) in polar form with a positive radius and an angle in [0, 2π). Explain the angle. This is an extra practice check, not a hand-in question.</p><div style="height:10mm"></div></div>${foot(3,'Student copy')}</section>`];
const formulas={A1:'2-i\\leftrightarrow(2,-1),\\quad r=\\sqrt5,\\quad\\theta=-\\arctan(1/2)',A2:'x=(8,0),\\qquad 2y=u+w,\\qquad y=(5/2,-1)',A3:'x=5(\\cos(\\pi/4),\\sin(\\pi/4))=(5/\\sqrt2,5/\\sqrt2)',A4:'(3,0)=3u(0),\\qquad(0,-3)=3u(3\\pi/2)',A5:'\\frac{13}{1+i}=\\frac{13(1-i)}{(1+i)(1-i)}=\\frac{13}2-\\frac{13}2i',A6:'a\\cdot b=-10,\\quad\\|a\\|=\\sqrt5,\\quad\\|b\\|=10,\\quad\\cos\\phi=-1/\\sqrt5'};
const teacher=script.filter(s=>s.boardGuide).map((s,i)=>{const e=ref.find(e=>e.id===s.topic),g=s.boardGuide;return `<section class="page">${head(e.id+' · '+e.title,'TEACHER ONLY — contains solutions | 先让学生尝试，再按顺序画图与补答案')}<div class="question">${q(e.id)}</div><div class="diagram">${svg(g.diagram)}</div><div class="formula">${katex.renderToString(formulas[e.id],{displayMode:true,throwOnError:true})}</div><h2>Board sequence · 板书顺序</h2><ol>${g.steps.map(x=>`<li><div>${rich(x.en)}</div><div class="zh">${rich(x.zh)}</div></li>`).join('')}</ol><div class="notice"><strong>Ask before explaining:</strong> ${rich(s.quick.check.en)}<div class="zh">${rich(s.quick.check.cn)}</div></div>${foot(i+1,'Tutor copy — do not distribute')}</section>`});
fs.writeFileSync(out+'/student-worksheet.html',shell('Linear Algebra Week 1 — Student worksheet',student));fs.writeFileSync(out+'/tutor-board-guide.html',shell('Linear Algebra Week 1 — Tutor board guide',teacher,true));console.log(JSON.stringify({studentPages:student.length,tutorPages:teacher.length,output:rel}));
