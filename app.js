(()=>{'use strict';history.scrollRestoration='manual';const q=s=>document.querySelector(s),all=s=>[...document.querySelectorAll(s)];const lessons=all('.lesson');const key='pure-teaching-v1';let prefs={index:1,cn:true,size:25};try{prefs={...prefs,...JSON.parse(localStorage.getItem(key)||'{}')}}catch{}let index=1;const save=()=>{try{localStorage.setItem(key,JSON.stringify({...prefs,index}))}catch{}};
function show(n,focus=false){index=Math.max(0,Math.min(lessons.length-1,Number(n)||0));lessons.forEach((el,i)=>el.hidden=i!==index);all('[data-go]').forEach(el=>{const on=Number(el.dataset.go)===index;el.classList.toggle('active',on);if(on)el.setAttribute('aria-current','step');else el.removeAttribute('aria-current')});q('#section-select').value=index;q('#prev').disabled=index===0;q('#next').disabled=index===lessons.length-1;q('#position').textContent=`${index} / ${lessons.length-1}`;history.replaceState(null,'',`#step-${index}`);save();window.scrollTo({top:0,behavior:'instant'});if(focus)lessons[index].querySelector('h1').focus({preventScroll:true})}
function renderPrefs(){document.body.classList.toggle('hide-cn',!prefs.cn);q('#cn-toggle').setAttribute('aria-pressed',String(prefs.cn));q('#cn-toggle').textContent=`中文对照 · ${prefs.cn?'开':'关'}`;document.documentElement.style.setProperty('--read-size',prefs.size+'px');q('#font-down').disabled=prefs.size<=18;q('#font-up').disabled=prefs.size>=34;save()}
all('[data-go]').forEach(b=>b.addEventListener('click',()=>show(b.dataset.go,true)));q('#section-select').addEventListener('change',e=>show(e.target.value,true));q('#prev').onclick=()=>show(index-1,true);q('#next').onclick=()=>show(index+1,true);q('#cn-toggle').onclick=()=>{prefs.cn=!prefs.cn;renderPrefs()};q('#font-up').onclick=()=>{prefs.size=Math.min(34,prefs.size+2);renderPrefs()};q('#font-down').onclick=()=>{prefs.size=Math.max(18,prefs.size-2);renderPrefs()};document.addEventListener('keydown',e=>{if(/INPUT|SELECT|TEXTAREA/.test(e.target.tagName))return;if(e.key==='ArrowRight'){e.preventDefault();show(index+1,true)}if(e.key==='ArrowLeft'){e.preventDefault();show(index-1,true)}});let t={remaining:3000,running:false,end:0};try{t={...t,...JSON.parse(localStorage.getItem(key+'-timer')||'{}')}}catch{}const saveT=()=>{try{localStorage.setItem(key+'-timer',JSON.stringify(t))}catch{}};let wake=null;async function keepAwake(){try{if(t.running&&'wakeLock'in navigator)wake=await navigator.wakeLock.request('screen')}catch{}}function tick(){let left=t.running?Math.max(0,Math.ceil((t.end-Date.now())/1000)):t.remaining;if(left===0&&t.running){t={remaining:0,running:false,end:0};saveT();wake?.release()}q('#clock').textContent=`${String(Math.floor(left/60)).padStart(2,'0')}:${String(left%60).padStart(2,'0')}`;q('#clock').classList.toggle('ending',left<=180);q('#timer-toggle').textContent=t.running?'暂停计时':left===3000?'开始 50 分钟':left===0?'重新开始':'继续计时'}q('#timer-toggle').onclick=()=>{if(t.running){t.remaining=Math.max(0,Math.ceil((t.end-Date.now())/1000));t.running=false;wake?.release()}else{if(t.remaining<=0)t.remaining=3000;t.running=true;t.end=Date.now()+t.remaining*1000;keepAwake()}saveT();tick()};q('#timer-reset').onclick=()=>{t={remaining:3000,running:false,end:0};saveT();wake?.release();tick()};document.addEventListener('visibilitychange',()=>{if(!document.hidden)keepAwake()});setInterval(tick,500);tick();const hash=location.hash.match(/^#step-(\d+)$/);show(hash?+hash[1]:prefs.index);renderPrefs();document.fonts.ready.then(()=>window.scrollTo(0,0));
const badge=q('#offline');if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').then(()=>navigator.serviceWorker.ready).then(()=>{badge.textContent=navigator.onLine?'离线已就绪':'离线可用';badge.dataset.ready='true'}).catch(()=>{badge.textContent='需联网查看'})}else badge.textContent='需联网查看';window.addEventListener('offline',()=>{badge.textContent=badge.dataset.ready?'离线可用':'当前无网络'});window.addEventListener('online',()=>{badge.textContent=badge.dataset.ready?'离线已就绪':'正在准备离线'});
})();
